# 🎨 Portfolio References & Inspiration
## Curated Collection for Igor Luiz's Portfolio — 2025/2026

---

## 📌 PART 1: Best Portfolio Websites (15+ Examples)

### 1. Brittany Chiang — brittanychiang.com
- **URL:** https://brittanychiang.com
- **What makes it special:** The gold standard for developer portfolios. Clean side-by-side layout (sticky sidebar nav + scrolling content). Dark theme with green/teal accents. Minimal but effective — no excessive animations, just pure content clarity.
- **Techniques:** Gatsby/Next.js, React, CSS Modules, smooth scroll anchor navigation
- **Why it works for Igor:** Proves that simplicity + great content beats flashy animations. The sticky sidebar pattern works perfectly for showcasing experience + projects.

### 2. Sidewave — sidewave.dev (Awwwards SOTD, May 2026)
- **URL:** https://sidewave.dev
- **What makes it special:** Awwwards Site of the Day. System-driven design merging structure, motion, and technology. Pure black/white palette. Features: hero section with video, text animations, mouse interactions, page transitions, horizontal scroll, AI chatbot integration, 3D elements, WebGL.
- **Techniques:** WebGL, Unity, HTML5, 3D rendering, page transitions, horizontal scroll
- **Why it works for Igor:** The AI chatbot element is directly relevant to Igor's services. The black/white palette with bold typography is a strong reference for dark theme.

### 3. Bruno Cisco — brunocisco.com (Awwwards Nominee, May 2026)
- **URL:** https://brunocisco.com
- **What makes it special:** Amsterdam-based creative copywriter portfolio. Minimal design with storytelling focus. Copy-design approach where text IS the design. Clean project pages with strong narrative flow.
- **Techniques:** CSS, JavaScript, Figma. Minimal tech stack, maximum impact.
- **Why it works for Igor:** Shows that great copywriting and storytelling matter more than tech stack. Relevant for crafting Igor's service descriptions.

### 4. Jacek Jeźnach — jacekjeznach.com
- **URL:** https://jacekjeznach.com
- **What makes it special:** Award-winning developer portfolio with dramatic dark theme. Bold typography, smooth scroll animations, custom cursor effects, and impressive project showcases with interactive elements.
- **Techniques:** GSAP, ScrollTrigger, custom cursor, smooth scrolling, WebGL effects
- **Why it works for Igor:** Dark theme with strong visual identity. Great reference for section transitions and project presentation.

### 5. Rob Curnow — robcurnow.com
- **URL:** https://robcurnow.com
- **What makes it special:** Creative developer portfolio with immersive 3D experiences, scroll-driven animations, and bold visual storytelling. Premium feel with dark backgrounds and glowing accents.
- **Techniques:** Three.js, GSAP, WebGL, custom shaders, scroll-triggered animations
- **Why it works for Igor:** Shows how 3D elements can elevate a portfolio without being overwhelming.

### 6. Obys Agency — obys.io (Awwwards DEV + SOTD)
- **URL:** https://obys.io
- **What makes it special:** Ukrainian design agency. Known for incredible scroll animations, custom page transitions, morphing elements, and creative typography. Multiple Awwwards wins.
- **Techniques:** GSAP, ScrollTrigger, custom page transitions, SVG morphing, split text animations
- **Why it works for Igor:** Gold standard for animation-heavy portfolios. The scroll-triggered reveals and text animations are directly applicable.

### 7. Gymnase Studio — gymnase.studio (Awwwards Portfolio Winner)
- **URL:** https://gymnase.studio
- **What makes it special:** French web agency with stunning portfolio. Bold colors, creative grid layouts, smooth transitions between projects, and interactive hover states.
- **Techniques:** GSAP, custom transitions, creative grid systems, hover interactions
- **Why it works for Igor:** Great reference for project grid layouts and hover effects.

### 8. DashDigital — dashdigital.io (Awwwards Winner)
- **URL:** https://dashdigital.io
- **What makes it special:** Digital agency portfolio with dark theme, premium feel, and impressive project showcases. Features smooth scrolling, parallax effects, and elegant typography.
- **Techniques:** GSAP, parallax scrolling, smooth transitions, dark UI design
- **Why it works for Igor:** Premium dark theme reference. The dashboard-style layout could inspire how Igor presents his dashboard/automation work.

### 9. Aino Agency — aino.agency (Awwwards DEV + SOTD)
- **URL:** https://aino.agency
- **What makes it special:** Finnish digital agency. Clean Scandinavian design with subtle animations. Excellent use of white space, typography, and micro-interactions.
- **Techniques:** Subtle GSAP animations, clean CSS transitions, thoughtful micro-interactions
- **Why it works for Igor:** Proves that restraint can be powerful. Great reference for professional agency-style presentation.

### 10. Matvey An — matveyan.com (Awwwards Winner)
- **URL:** https://matveyan.com
- **What makes it special:** Designer portfolio with creative layout experiments, bold color usage, and interactive elements. Strong personality-driven design.
- **Techniques:** Creative CSS layouts, GSAP animations, interactive elements
- **Why it works for Igor:** Shows how personality can shine through in portfolio design.

### 11. Nathan Rodriguez — nathanrodriguez.com (Awwwards HM)
- **URL:** https://nathanrodriguez.com
- **What makes it special:** Developer/creative portfolio with dark theme, smooth animations, and clean project presentations. Honorable Mention on Awwwards.
- **Techniques:** Modern JS frameworks, smooth transitions, dark UI
- **Why it works for Igor:** Clean developer-focused portfolio with professional presentation.

### 12. Synapser Studio — synapser.studio (Awwwards Winner)
- **URL:** https://synapser.studio
- **What makes it special:** Tech-focused creative studio with futuristic dark theme, glowing effects, and interactive 3D elements. Strong tech aesthetic.
- **Techniques:** Three.js, WebGL, glowing effects, dark theme with neon accents
- **Why it works for Igor:** Tech-forward aesthetic that aligns with automation/chatbot services.

### 13. Studio Namma — studionamma.com (Awwwards DEV + SOTD)
- **URL:** https://studionamma.com
- **What makes it special:** Award-winning studio portfolio with creative interactions, scroll-triggered animations, and unique navigation patterns.
- **Techniques:** GSAP, ScrollTrigger, creative navigation, scroll animations
- **Why it works for Igor:** Reference for creative navigation and scroll interactions.

### 14. Codedgar — codedgar.com (Awwwards Winner)
- **URL:** https://codedgar.com
- **What makes it special:** Developer portfolio with personality. Shows how developers can stand out with creative design while maintaining technical credibility.
- **Techniques:** Creative CSS, animations, personal branding
- **Why it works for Igor:** Direct competitor reference — another developer showcasing skills creatively.

### 15. Victor Furuya — victorfuruya.com (Awwwards HM, 2026)
- **URL:** https://victorfuruya.com
- **What makes it special:** 2026 portfolio with modern design patterns, smooth interactions, and clean layout. Recent Awwwards Honorable Mention.
- **Techniques:** Modern web technologies, smooth animations, clean design
- **Why it works for Igor:** Current 2026 design trends reference.

### 16. Simonholm Studio — simonholm.studio (Awwwards SOTD)
- **URL:** https://simonholm.studio
- **What makes it special:** Scandinavian design studio with minimal aesthetic, perfect typography, and subtle but impactful animations. Site of the Day winner.
- **Techniques:** Minimal animations, typography-driven design, clean layouts
- **Why it works for Igor:** Shows that minimalism + great typography = award-winning design.

### 17. POWER SHIFTER — powershifter.com (Awwwards Winner)
- **URL:** https://powershifter.com
- **What makes it special:** "Resurrection" themed portfolio with dark, dramatic presentation. Strong visual storytelling and immersive experience.
- **Techniques:** Dark theme, dramatic animations, immersive scrolling
- **Why it works for Igor:** Reference for dramatic, impactful dark-themed presentations.

### 18. Aeruk — aeruk.com (Awwwards HM)
- **URL:** https://aeruk.com
- **What makes it special:** Creative portfolio with unique visual identity and interactive elements. Awwwards Honorable Mention.
- **Techniques:** Custom interactions, creative layouts, modern web tech
- **Why it works for Igor:** Reference for unique visual identity in portfolio design.

---

## 🎭 PART 2: Animation Techniques & Code Examples

### 2.1 Text Reveal Animations

#### Split Text (Word-by-Word)
```jsx
// Using GSAP SplitText plugin
import { gsap } from "gsap";
import { SplitText } from "gsap/SplitText";

// Split text into words
const split = new SplitText(".hero-title", { type: "words" });

gsap.from(split.words, {
  y: 100,
  opacity: 0,
  rotationX: -90,
  stagger: 0.05,
  duration: 0.8,
  ease: "back.out(1.7)"
});
```

#### Character-by-Character Reveal
```jsx
// React + Framer Motion approach
import { motion } from "framer-motion";

const TextReveal = ({ text }) => {
  const characters = text.split("");
  
  return (
    <h1 className="hero-title">
      {characters.map((char, i) => (
        <motion.span
          key={i}
          initial={{ opacity: 0, y: 50 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{
            duration: 0.5,
            delay: i * 0.03,
            ease: [0.22, 1, 0.36, 1]
          }}
          style={{ display: "inline-block" }}
        >
          {char === " " ? "\u00A0" : char}
        </motion.span>
      ))}
    </h1>
  );
};
```

#### CSS-Only Text Reveal
```css
@keyframes textReveal {
  0% {
    clip-path: polygon(0 0, 0 0, 0 100%, 0 100%);
  }
  100% {
    clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
  }
}

.reveal-text {
  animation: textReveal 0.8s cubic-bezier(0.77, 0, 0.175, 1) forwards;
}

/* Staggered by nth-child */
.reveal-text:nth-child(2) { animation-delay: 0.1s; }
.reveal-text:nth-child(3) { animation-delay: 0.2s; }
```

### 2.2 Smooth Scroll (Lenis)

```jsx
// Using Lenis for smooth scrolling
import Lenis from "@studio-freight/lenis";
import { useEffect } from "react";
import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

function App() {
  useEffect(() => {
    const lenis = new Lenis({
      duration: 1.2,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      direction: "vertical",
      gestureDirection: "vertical",
      smooth: true,
      smoothTouch: false,
      touchMultiplier: 2,
    });

    // Connect Lenis to GSAP ScrollTrigger
    lenis.on("scroll", ScrollTrigger.update);

    gsap.ticker.add((time) => {
      lenis.raf(time * 1000);
    });

    gsap.ticker.lagSmoothing(0);

    return () => {
      lenis.destroy();
    };
  }, []);

  return <div className="app">{/* Your content */}</div>;
}
```

### 2.3 Cursor Follower & Magnetic Buttons

#### Custom Cursor Follower
```jsx
import { useEffect, useRef } from "react";
import { gsap } from "gsap";

const CursorFollower = () => {
  const cursorRef = useRef(null);
  const cursorDotRef = useRef(null);

  useEffect(() => {
    const cursor = cursorRef.current;
    const dot = cursorDotRef.current;

    const moveCursor = (e) => {
      gsap.to(cursor, {
        x: e.clientX,
        y: e.clientY,
        duration: 0.5,
        ease: "power3.out"
      });
      gsap.to(dot, {
        x: e.clientX,
        y: e.clientY,
        duration: 0.1
      });
    };

    // Scale up on hover over interactive elements
    const handleMouseEnter = () => {
      gsap.to(cursor, { scale: 2, duration: 0.3 });
    };
    const handleMouseLeave = () => {
      gsap.to(cursor, { scale: 1, duration: 0.3 });
    };

    document.addEventListener("mousemove", moveCursor);
    document.querySelectorAll("a, button").forEach(el => {
      el.addEventListener("mouseenter", handleMouseEnter);
      el.addEventListener("mouseleave", handleMouseLeave);
    });

    return () => {
      document.removeEventListener("mousemove", moveCursor);
    };
  }, []);

  return (
    <>
      <div ref={cursorRef} className="cursor-ring" />
      <div ref={cursorDotRef} className="cursor-dot" />
    </>
  );
};

/* CSS */
.cursor-ring {
  position: fixed;
  width: 40px;
  height: 40px;
  border: 2px solid rgba(255, 255, 255, 0.5);
  border-radius: 50%;
  pointer-events: none;
  transform: translate(-50%, -50%);
  z-index: 9999;
  mix-blend-mode: difference;
}
.cursor-dot {
  position: fixed;
  width: 8px;
  height: 8px;
  background: white;
  border-radius: 50%;
  pointer-events: none;
  transform: translate(-50%, -50%);
  z-index: 9999;
}
```

#### Magnetic Buttons
```jsx
import { useRef } from "react";
import { gsap } from "gsap";

const MagneticButton = ({ children }) => {
  const buttonRef = useRef(null);

  const handleMouseMove = (e) => {
    const { clientX, clientY } = e;
    const { left, top, width, height } = buttonRef.current.getBoundingClientRect();
    const x = clientX - (left + width / 2);
    const y = clientY - (top + height / 2);

    gsap.to(buttonRef.current, {
      x: x * 0.3,
      y: y * 0.3,
      duration: 0.3,
      ease: "power2.out"
    });
  };

  const handleMouseLeave = () => {
    gsap.to(buttonRef.current, {
      x: 0,
      y: 0,
      duration: 0.5,
      ease: "elastic.out(1, 0.5)"
    });
  };

  return (
    <button
      ref={buttonRef}
      onMouseMove={handleMouseMove}
      onMouseLeave={handleMouseLeave}
      className="magnetic-btn"
    >
      {children}
    </button>
  );
};
```

### 2.4 Page Transitions (View Transitions API)

```jsx
// Modern approach: View Transitions API (native browser API)
function navigateTo(url) {
  if (!document.startViewTransition) {
    window.location.href = url;
    return;
  }

  document.startViewTransition(async () => {
    const response = await fetch(url);
    const html = await response.text();
    const parser = new DOMParser();
    const doc = parser.parseFromString(html, "text/html");
    document.body.innerHTML = doc.body.innerHTML;
    window.history.pushState({}, "", url);
  });
}

/* CSS for view transitions */
::view-transition-old(root) {
  animation: fade-out 0.3s ease-out;
}
::view-transition-new(root) {
  animation: fade-in 0.3s ease-in;
}

@keyframes fade-out {
  from { opacity: 1; transform: scale(1); }
  to { opacity: 0; transform: scale(0.95); }
}
@keyframes fade-in {
  from { opacity: 0; transform: scale(1.05); }
  to { opacity: 1; transform: scale(1); }
}
```

#### Barba.js Page Transitions
```jsx
import barba from "@barba/core";
import { gsap } from "gsap";

barba.init({
  transitions: [{
    name: "fade",
    leave(data) {
      return gsap.to(data.current.container, {
        opacity: 0,
        y: -50,
        duration: 0.5,
        ease: "power2.inOut"
      });
    },
    enter(data) {
      return gsap.from(data.next.container, {
        opacity: 0,
        y: 50,
        duration: 0.5,
        ease: "power2.inOut"
      });
    }
  }]
});
```

### 2.5 Staggered Grid Animations

```jsx
// GSAP ScrollTrigger staggered grid
import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
import { useEffect, useRef } from "react";

const StaggeredGrid = () => {
  const gridRef = useRef(null);

  useEffect(() => {
    const items = gridRef.current.querySelectorAll(".grid-item");

    gsap.from(items, {
      scrollTrigger: {
        trigger: gridRef.current,
        start: "top 80%",
        end: "bottom 20%",
        toggleActions: "play none none reverse"
      },
      y: 100,
      opacity: 0,
      scale: 0.8,
      stagger: {
        amount: 0.6,
        from: "start",
        grid: "auto"
      },
      duration: 0.8,
      ease: "back.out(1.4)"
    });
  }, []);

  return (
    <div ref={gridRef} className="projects-grid">
      {projects.map((project, i) => (
        <div key={i} className="grid-item">
          <ProjectCard project={project} />
        </div>
      ))}
    </div>
  );
};
```

### 2.6 Parallax Depth Effects

```jsx
// Multi-layer parallax with GSAP
import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

useEffect(() => {
  // Background layer - moves slower
  gsap.to(".parallax-bg", {
    scrollTrigger: {
      trigger: ".section",
      start: "top bottom",
      end: "bottom top",
      scrub: true
    },
    y: -100,
    ease: "none"
  });

  // Middle layer - normal speed
  gsap.to(".parallax-mid", {
    scrollTrigger: {
      trigger: ".section",
      start: "top bottom",
      end: "bottom top",
      scrub: true
    },
    y: -200,
    ease: "none"
  });

  // Foreground layer - moves faster
  gsap.to(".parallax-fg", {
    scrollTrigger: {
      trigger: ".section",
      start: "top bottom",
      end: "bottom top",
      scrub: true
    },
    y: -300,
    ease: "none"
  });
}, []);

/* CSS */
.parallax-container {
  position: relative;
  overflow: hidden;
  height: 100vh;
}
.parallax-bg { position: absolute; z-index: 1; }
.parallax-mid { position: absolute; z-index: 2; }
.parallax-fg { position: absolute; z-index: 3; }
```

### 2.7 Morphing SVG Paths

```jsx
// Using GSAP MorphSVG plugin
import { gsap } from "gsap";
import { MorphSVGPlugin } from "gsap/MorphSVGPlugin";

gsap.registerPlugin(MorphSVGPlugin);

// Morph from circle to star
gsap.to("#circle-path", {
  morphSVG: "#star-path",
  duration: 1,
  ease: "power2.inOut"
});

// Manual SVG path morphing (free alternative)
const morphSVG = (fromPath, toPath, duration = 1) => {
  const from = fromPath.getAttribute("d");
  const to = toPath.getAttribute("d");
  
  // Use flubber library for path interpolation
  const interpolator = flubber.interpolate(from, to);
  
  gsap.to({ t: 0 }, {
    t: 1,
    duration,
    ease: "power2.inOut",
    onUpdate: function() {
      fromPath.setAttribute("d", interpolator(this.targets()[0].t));
    }
  });
};

/* SVG morphing with CSS (simple shapes) */
.morph-shape {
  clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
  transition: clip-path 0.6s cubic-bezier(0.65, 0, 0.35, 1);
}
.morph-shape:hover {
  clip-path: polygon(20% 0%, 80% 0%, 100% 100%, 0% 100%);
}
```

### 2.8 3D Card Tilt Effects

```jsx
import { useRef } from "react";
import { gsap } from "gsap";

const TiltCard = ({ children }) => {
  const cardRef = useRef(null);

  const handleMouseMove = (e) => {
    const { left, top, width, height } = cardRef.current.getBoundingClientRect();
    const x = (e.clientX - left) / width;
    const y = (e.clientY - top) / height;

    const rotateX = (y - 0.5) * 20; // -10 to 10 degrees
    const rotateY = (x - 0.5) * -20;

    gsap.to(cardRef.current, {
      rotateX,
      rotateY,
      transformPerspective: 1000,
      duration: 0.3,
      ease: "power2.out"
    });

    // Move shine effect
    gsap.to(".card-shine", {
      background: `radial-gradient(circle at ${x * 100}% ${y * 100}%, rgba(255,255,255,0.3), transparent 60%)`,
      duration: 0.3
    });
  };

  const handleMouseLeave = () => {
    gsap.to(cardRef.current, {
      rotateX: 0,
      rotateY: 0,
      duration: 0.5,
      ease: "elastic.out(1, 0.5)"
    });
  };

  return (
    <div
      ref={cardRef}
      onMouseMove={handleMouseMove}
      onMouseLeave={handleMouseLeave}
      className="tilt-card"
      style={{ transformStyle: "preserve-3d" }}
    >
      <div className="card-shine" />
      {children}
    </div>
  );
};

/* CSS */
.tilt-card {
  perspective: 1000px;
  transform-style: preserve-3d;
  will-change: transform;
  position: relative;
  overflow: hidden;
  border-radius: 16px;
  background: linear-gradient(135deg, #1a1a2e, #16213e);
}
.card-shine {
  position: absolute;
  inset: 0;
  z-index: 1;
  pointer-events: none;
  border-radius: inherit;
}
```

---

## 🎨 PART 3: Dark Theme Design Patterns

### Color Palettes for Dark Portfolios

#### Premium Dark + Gold/Amber (Recommended for Igor)
```css
:root {
  --bg-primary: #0a0a0a;
  --bg-secondary: #111111;
  --bg-card: #1a1a1a;
  --text-primary: #f5f5f5;
  --text-secondary: #a0a0a0;
  --accent: #d4a853;        /* Gold */
  --accent-hover: #e8c373;  /* Light gold */
  --accent-glow: rgba(212, 168, 83, 0.3);
  --border: rgba(255, 255, 255, 0.08);
}
```

#### Dark + Electric Blue (Tech Feel)
```css
:root {
  --bg-primary: #0a0a0f;
  --bg-secondary: #12121a;
  --accent: #4f7df5;
  --accent-hover: #6b93f7;
  --accent-glow: rgba(79, 125, 245, 0.3);
}
```

#### Dark + Emerald (Modern)
```css
:root {
  --bg-primary: #0a0f0a;
  --bg-secondary: #111a11;
  --accent: #10b981;
  --accent-hover: #34d399;
  --accent-glow: rgba(16, 185, 129, 0.3);
}
```

### Dark Theme Best Practices
1. **Never use pure black (#000)** — use #0a0a0a or #111 for softer feel
2. **Use subtle gradients** for depth (not flat colors)
3. **Add noise texture** for premium feel: `background-image: url("data:image/svg+xml,...")`
4. **Border treatment:** Use `rgba(255,255,255,0.06-0.1)` for subtle borders
5. **Glow effects:** Box-shadow with accent color at 20-30% opacity
6. **Text hierarchy:** Primary #f5f5f5, Secondary #a0a0a0, Muted #666

### Adding Noise Texture (Premium Feel)
```css
body::after {
  content: "";
  position: fixed;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
  pointer-events: none;
  z-index: 9999;
  opacity: 0.4;
}
```

---

## ✨ PART 4: Micro-Interactions Reference

### Hover Effects
```css
/* Underline grow from center */
.link-hover::after {
  content: "";
  position: absolute;
  bottom: -2px;
  left: 50%;
  width: 0;
  height: 2px;
  background: var(--accent);
  transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  transform: translateX(-50%);
}
.link-hover:hover::after {
  width: 100%;
}

/* Button glow on hover */
.btn-glow {
  position: relative;
  overflow: hidden;
  transition: all 0.3s ease;
}
.btn-glow::before {
  content: "";
  position: absolute;
  inset: -2px;
  background: linear-gradient(45deg, var(--accent), transparent, var(--accent));
  opacity: 0;
  transition: opacity 0.3s;
  z-index: -1;
  filter: blur(10px);
}
.btn-glow:hover::before {
  opacity: 1;
}

/* Card lift with shadow */
.card-lift {
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.card-lift:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
}
```

### Loading States
```css
/* Skeleton loading */
.skeleton {
  background: linear-gradient(
    90deg,
    var(--bg-card) 25%,
    var(--bg-secondary) 50%,
    var(--bg-card) 75%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}
@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

/* Spinner */
.spinner {
  width: 40px;
  height: 40px;
  border: 3px solid var(--border);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}
```

---

## 🛠 PART 5: Recommended Tech Stack for Igor's Portfolio

### Core Stack
- **Framework:** Next.js 14+ (App Router) or Astro (for static + islands)
- **Styling:** Tailwind CSS + custom CSS for animations
- **Animations:** GSAP + ScrollTrigger (industry standard)
- **Smooth Scroll:** Lenis (by Studio Freight)
- **3D (optional):** Three.js / React Three Fiber (only if needed)
- **Deployment:** Vercel (free, fast, Brazilian CDN edge)

### Animation Libraries Comparison
| Library | Best For | Bundle Size | Learning Curve |
|---------|----------|-------------|----------------|
| GSAP | Complex timelines, ScrollTrigger | ~30KB | Medium |
| Framer Motion | React-specific, declarative | ~30KB | Easy |
| Motion One | Web Animations API based | ~6KB | Easy |
| Anime.js | Simple animations | ~17KB | Easy |
| Lottie | After Effects animations | ~50KB | Medium |

### Key Libraries to Install
```bash
npm install gsap @studio-freight/lenis framer-motion
# Optional:
npm install three @react-three/fiber @react-three/drei
npm install @barba/core
```

---

## 📐 PART 6: Layout Patterns for Developer Portfolios

### Pattern 1: Side-by-Side (Brittany Chiang style)
```
┌──────────────────────────────────────┐
│  FIXED SIDEBAR  │  SCROLLING CONTENT │
│                 │                    │
│  Name           │  About             │
│  Title          │  Experience        │
│  Nav Links      │  Projects          │
│  Social Links   │  Contact           │
│                 │                    │
└──────────────────────────────────────┘
```
**Best for:** Content-heavy portfolios, developers with lots of experience

### Pattern 2: Full-Screen Sections
```
┌──────────────────┐
│   HERO (100vh)   │
├──────────────────┤
│   ABOUT (100vh)  │
├──────────────────┤
│  PROJECTS (auto) │
├──────────────────┤
│  CONTACT (100vh) │
└──────────────────┘
```
**Best for:** Visual-heavy portfolios, creative developers

### Pattern 3: Horizontal Scroll
```
┌──────────────────────────────────────┐
│  HERO                                 │
├────┬────┬────┬────┬────┬────┬────────┤
│ P1 │ P2 │ P3 │ P4 │ P5 │ P6 │ CONTACT│
└────┴────┴────┴────┴────┴────┴────────┘
```
**Best for:** Showcasing many projects visually

### Pattern 4: Single Page with Sticky Nav
```
┌──────────────────────────────────────┐
│  NAV (sticky, blur backdrop)         │
├──────────────────────────────────────┤
│  HERO with animated text             │
│  ↓ scroll                            │
│  SERVICES (grid cards)               │
│  ↓ scroll                            │
│  PROJECTS (filterable grid)          │
│  ↓ scroll                            │
│  TESTIMONIALS (carousel)             │
│  ↓ scroll                            │
│  CONTACT (form + info)               │
└──────────────────────────────────────┘
```
**Best for:** Freelancer portfolios targeting clients (RECOMMENDED FOR IGOR)

---

## 🎯 PART 7: Specific Recommendations for Igor

### Target Audience: Small Business Owners in Brazil
- **Language:** Portuguese (BR) with English option
- **Tone:** Professional but approachable
- **Focus:** Results-oriented (show ROI, automation savings, chatbot conversions)
- **Trust signals:** Client testimonials, case studies, WhatsApp contact

### Suggested Sections
1. **Hero:** "Automatizo processos e crio soluções web que vendem" + CTA button
2. **Serviços:** React, Automação, Chatbots, Dashboards (icon cards)
3. **Projetos:** 4-6 case studies with before/after metrics
4. **Sobre:** Brief personal story + photo
5. **Depoimentos:** Client testimonials (social proof)
6. **Contato:** WhatsApp button (primary) + form

### Color Recommendation
Use the **Dark + Gold/Amber** palette — it conveys premium quality and trust, which is important for small business owners investing in technology.

### Animation Strategy
- **Keep it subtle** — business owners want to see competence, not flashy effects
- **Use scroll reveals** for content sections (builds anticipation)
- **Animated counters** for metrics (e.g., "+50 projetos entregues", "300% ROI médio")
- **Smooth page transitions** between project case studies
- **Cursor effects** only on desktop, disabled on mobile

---

## 📚 Additional Resources

### Inspiration Galleries
- https://www.awwwards.com/websites/portfolio/ — 9,344+ portfolio examples
- https://www.cssdesignawards.com — CSS-focused awards
- https://www.siteinspire.com — Curated web design inspiration
- https://onepagelove.com — One-page website inspiration
- https://www.behance.net/search/projects?search=portfolio-web-design

### Learning Resources
- https://www.awwwards.com/academy — Web animation courses
- https://greensock.com/docs/ — GSAP documentation
- https://lenis.darkroom.engineering/ — Lenis smooth scroll
- https://www.framer.com/motion/ — Framer Motion docs
- https://threejs-journey.com — Three.js course by Bruno Simon

### Code References
- https://codepen.io/trending — Animation code examples
- https://github.com/nicedoc/awesome-portfolio — Portfolio GitHub repos
- https://github.com/bradtraversy/design-resources-for-developers — Design resources

---

*Last updated: May 23, 2026*
*Compiled for: Igor Luiz — Freelance Developer Portfolio*
